# ------------------------------------------------------------------ STACK PUSH
sp                      := $(sp).x
dirstack_$(sp)          := $(d)
d                       := $(dir)

# ------------------------------------------------------------- LOCAL VARIABLES


# ----------------------------------------------------------------- LOCAL RULES

dir                     := $(d)/gmockgtest
include $(dir)/Rules.mk


# ------------------------------------------------------------------- POP STACK
d                       := $(dirstack_$(sp))
sp                      := $(basename $(sp))
